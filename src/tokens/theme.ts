import {
  color,
  fontFamily,
  fontWeight,
  fontSize,
  lineHeight,
  breakpoint,
  spacing,
} from './tokens';

export const theme = {
  color,
  fontFamily,
  fontWeight,
  fontSize,
  lineHeight,
  breakpoint,
  spacing,
};

export type AppTheme = typeof theme;
